build/*
install/*
log/*
site/*

# micro-ROS agent sibling workspace, materialized by
# scripts/setup-uros-agent.sh via `ros2 run micro_ros_setup create_agent_ws.sh`.
uros_agent_ws/

# CoPilot CLI worktrees
src/esp32-s3-uros-controller.worktrees

# NTRIP caster credentials — copy ntrip.yaml.example to ntrip.yaml and fill in.
# Never commit real credentials.
**/ntrip.yaml